apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ghcr-test
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ghcr-test
  filterTags:
    pattern: '^[0-9]+$'
  policy:
    numerical:
      order: asc
