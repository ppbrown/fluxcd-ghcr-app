apiVersion: apps/v1
kind: Deployment
metadata:
  name: ghcr-app
  namespace: ghcr-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ghcr-app
  template:
    metadata:
      labels:
        app: ghcr-app
    spec:
      containers:
        - name: ghcr-test
          image: ghcr.io/ppbrown/ghcr-test:11 # {"$imagepolicy": "flux-system:ghcr-test"}
          ports:
            - containerPort: 8080
